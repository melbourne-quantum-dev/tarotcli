# tarotCLI Environment Variables
# Copy this file to .env and add your actual keys
# IMPORTANT: Never commit .env to git (included in .gitignore)

# Anthropic API Key (required for Claude provider)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override default provider via environment
# TAROTCLI_MODELS_DEFAULT_PROVIDER=ollama

# Optional: Custom data directory path
# TAROTCLI_DATA_DIR=/path/to/custom/data

# Optional: Override any config value using TAROTCLI_ prefix
# Example: TAROTCLI_OUTPUT_FORMAT=json
# Pattern: TAROTCLI_<SECTION>_<KEY>=value (nested keys use underscores)